name: Release

# 当推送一个形如 'v*' 的 tag 时触发，例如 v1.0.0
on:
  push:
    tags:
      - "v*"

jobs:
  check-tag:
    name: Check Tag Validity
    runs-on: ubuntu-latest
    outputs:
      is_valid: ${{ steps.check.outputs.is_valid }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check if tag is on master
        id: check
        shell: bash
        run: |
          # 直接利用 git 命令判断 tag 的 commit 是否在 master 历史中
          if git merge-base --is-ancestor "${{ github.ref_name }}" origin/master; then
            echo "Tag is on master branch."
            # 将结果输出给后续 job 使用
            echo "is_valid=true" >> $GITHUB_OUTPUT
          else
            echo "Tag is NOT on master branch. Aborting."
            echo "is_valid=false" >> $GITHUB_OUTPUT
          fi

  build:
    name: Build and Release
    needs: check-tag
    if: needs.check-tag.outputs.is_valid == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      # 工具基础名称（如需改名，请同时修改下面 matrix 中的 artifact_name 和 binary_name）
      # 参考平台支持：https://doc.rust-lang.org/nightly/rustc/platform-support.html#tier-1-with-host-tools
      matrix:
        include:
          # Windows
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: easy-media-cli-windows-x86_64.exe
            binary_name: easy-media-cli.exe
          # macOS Intel
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: easy-media-cli-macos-x86_64
            binary_name: easy-media-cli
          # macOS Apple Silicon
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: easy-media-cli-macos-aarch64
            binary_name: easy-media-cli
          # Linux x86_64
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: easy-media-cli-linux-x86_64
            binary_name: easy-media-cli

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0 # 需要完整历史以检查 tag 所在分支

      - name: Setup Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          target: ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Upload binary as artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: target/${{ matrix.target }}/release/${{ matrix.binary_name }}
          compression-level: 0

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Generate changelog
        uses: orhun/git-cliff-action@v4
        id: git-cliff
        with:
          args: --latest --strip header
        env:
          OUTPUT: CHANGELOG.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          # 下载到 artifacts/ 目录，每个 artifact 以其名称创建子目录
          path: artifacts

      # 将下载的各个二进制文件整理到一个扁平目录，并重命名为平台标识
      - name: Flatten and rename binaries
        run: |
          mkdir -p dist
          # 遍历 artifacts 下的所有子目录
          for dir in artifacts/*/; do
            # 去除末尾斜杠，获取目录名（即 artifact_name）
            dirname=$(basename "$dir")
            # 查找该目录下的第一个普通文件（通常只有一个）
            file=$(find "$dir" -type f -print -quit)
            if [ -n "$file" ]; then
              # 复制文件到 dist，并以目录名作为新文件名
              cp "$file" "dist/$dirname"
            else
              echo "Warning: No file found in $dir" >&2
            fi
          done
          # 列出 dist 内容供调试
          ls -l dist

      # 恢复可执行权限（对于 Linux/macOS 二进制文件）
      - name: Set executable permissions
        run: |
          chmod +x artifacts/*/easy-media-cli || true

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          body_path: CHANGELOG.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
